import book from "@/assets/brand/book.png";
import chatbot from "@/assets/brand/chatbot.png";
import studentMascot from "@/assets/brand/student.png";

/** Bundled brand images — reliable in dev, ngrok, and production builds. */
export const brandImages = {
  book,
  chatbot,
  studentMascot,
} as const;
